# =============================================================================
# URA Chatbot - App Dependencies
# Hugging Face Spaces Deployment
# =============================================================================

# Core ML
scikit-learn>=1.3.0
numpy>=1.26.0
joblib>=1.3.0

# Embeddings
sentence-transformers>=2.6.0
torch>=2.2.0

# Web Interface — Gradio 5 stable (SSR, improved streaming, accessibility)
gradio>=5.0.0,<6.0.0

# Hugging Face ZeroGPU ("free GPU" on Spaces). Optional: only used when the
# Space hardware is set to ZeroGPU. app.py guards the import, so CPU/local
# runs work without it installed.
spaces>=0.30.0

# Hugging Face Integration
huggingface-hub>=0.19.0
transformers>=4.35.0

# Data Processing
pandas>=2.0.0

# Utilities
python-dotenv>=1.0.0
