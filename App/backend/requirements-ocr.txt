# Optional local OCR sidecar.  Keep this separate from the API's base and
# Crane Cloud requirements: EasyOCR/Torch models belong in the OCR process.
fastapi>=0.141.1,<0.142
uvicorn[standard]>=0.52.4,<0.53
httpx>=0.28.1,<0.29
numpy>=2.5.2,<3
Pillow>=10.4
easyocr>=1.7.2,<2
