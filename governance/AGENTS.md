# governance/

| File | Role |
| --- | --- |
| `ai_risk_manifest.yaml` | NIST AI RMF + ISO 42001 + OWASP + EU AI Act mapping |
| `compliance_check.py` | CI gate — do not skip in merge |

Threat-model code lives in `threat-model/`. Security claims must match
`SECURITY.md` and the manifest, not marketing copy.
