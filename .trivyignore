# =============================================================================
# Trivy Ignore File – URA Chatbot
#
# Add CVE IDs or check IDs to suppress known false positives or accepted risks.
# Format: one ID per line, optional comment with # after.
# Docs: https://aquasecurity.github.io/trivy/latest/docs/configuration/filtering/#by-finding-ids
#
# Review this file periodically – accepted risks may become patchable.
# =============================================================================

# ── Accepted Risks (document reason and review date) ─────────────────────────
# Example:
# CVE-2024-XXXXX  # Low-impact in our context; no patch until upstream release. Review: 2026-06-01

# ── Misconfiguration Suppressions ────────────────────────────────────────────
# AVD-DS-0002 / DS0002: App/Dockerfile.cranecloud runs supervisord as PID 1
# to supervise multi-service stack (nginx, redis, qdrant, uvicorn, next.js) on
# Crane Cloud single-port container deployment.
# Compensating controls: CAP_DROP ALL, no-new-privileges, seccomp default profile,
# read-only rootfs with tmpfs mounts, unprivileged ports only. (Review: 2026-12-31)
AVD-DS-0002
DS0002

# ── License Suppressions ─────────────────────────────────────────────────────
# @img/sharp-libvips dynamically linked by sharp for Next.js blog image optimization.
# LGPL-3.0-or-later allows dynamic linking without copyleft viral effect on application code.
@img/sharp-libvips-linux-x64
@img/sharp-libvips-linuxmusl-x64
