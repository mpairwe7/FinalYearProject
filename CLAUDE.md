# Claude Code

Use the repo-wide router in [AGENTS.md](AGENTS.md). Nested `AGENTS.md` files
next to `App/backend`, `App/frontend`, `evals/`, `infra/`, and `governance/`
override only what is local to that tree.

Do not move `App/backend` or `App/frontend`.
